import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3">
        Should only be visible to logged in user!
    </Typography>
  )
}
